import { Container, Typography } from "@material-ui/core";
import Layout from "../../components/layout";

const QnADetails = () => {
  return (
    <Layout>
      <div style={{ marginTop: "6rem", minHeight: "75vh" }}>
        <Container>
          <Typography>this is qna details</Typography>
        </Container>
      </div>
    </Layout>
  );
};

export default QnADetails;
